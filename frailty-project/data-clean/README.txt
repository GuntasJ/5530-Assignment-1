The cleaned dataset differs from the dirty dataset in the following ways:

Height is measured in meters.

Weight is measured in kilograms.

BMI has been added.

Frailty has been encoded in binary.

Age has been made into categories, which have been converted using one-hot encoding. 
